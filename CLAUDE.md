# CLAUDE.md — 이동규 포트폴리오 프로젝트 컨텍스트

이 파일은 Claude가 이 폴더에서 작업할 때 항상 먼저 읽는 기준 문서입니다.
내용이 바뀌면 이 파일부터 갱신합니다.

---

## 1. 작업 원칙 (가장 중요)

1. **항상 한국어로 답변한다.**
2. **내용을 지어내지 않는다.** 포트폴리오에 들어가는 사실(수상, 수치, 역할, 기간, 성과, 데이터 출처, 용어 정의 등)은
   반드시 사용자가 제공한 자료나 폴더 안의 파일에서 확인된 것만 사용한다.
3. **모르는 것은 먼저 묻는다.** 자료에 없는 내용이 필요하면 추측으로 채우지 말고 질문한다.
   임시로 자리를 비워둬야 할 때는 `【확인 필요: 무엇을】` 표시를 남긴다.
4. **내용과 코드를 섞지 않는다.** 포트폴리오에 들어가는 글·수치는 모두 `backend/data/*.json` 에 있다.
   HTML·JS 파일에 문장을 직접 써넣지 않는다. (내용을 고칠 때 JSON만 고치면 되도록)
5. **코드는 나중에 고치기 쉽게 작성한다.**
   - 색상·글꼴·간격은 CSS 변수(`:root`)로만 관리하고 값을 직접 박아 넣지 않는다.
   - 섹션(페이지)마다 주석으로 시작과 끝을 표시한다. 예: `<!-- ===== [P03] 프로젝트: 에코빌딩 ===== -->`
   - 반복되는 카드·표는 같은 클래스 구조를 재사용한다.
   - 이미지·파일 이름은 **영문 소문자 + 하이픈**으로 짓는다 (한글·공백은 웹 주소에서 깨질 수 있음).
6. **목표 독자를 기억한다.** 채용 담당자와 현업 실무자가 전공 지식 없이도 한눈에 이해할 수 있어야 하고,
   동시에 전문적으로 보여야 한다. 전문 용어는 처음 나올 때 쉬운 설명을 함께 붙인다.
7. **팀원의 개인정보는 웹 페이지에 절대 반영하지 않는다.** (예외 없음)
   - 금지 대상: 팀원의 이름, 학번, 연락처, 사진, 그 밖에 개인을 알아볼 수 있는 모든 정보
   - 원본 자료(PPT·PDF 등)에 팀원 정보가 있어도 옮기지 않는다. 팀 프로젝트는 `3인 팀`처럼 **인원수만** 쓴다. 본인 역할도 기재하지 않는다.
   - 사진·캡처 이미지에 팀원 이름이나 학번이 보이면 가리거나 잘라낸 뒤에만 쓴다.
   - 이 CLAUDE.md를 포함해 공개 저장소에 올라가는 모든 파일에도 똑같이 적용한다.
   - 팀원 정보가 들어 있는 원본 파일(예: `에코빌딩 9조 15주차 최종.pptx`)은 공개 저장소에 올리지 않는다.

---

## 2. 포트폴리오 개요

| 항목 | 내용 |
|---|---|
| 용도 | **기업 지원용 포트폴리오** |
| 최종 형태 | **PDF** (웹 페이지로 만든 뒤 PDF로 변환 예정) |
| 관리 방식 | **페이지별로 내용을 나누어 관리** |
| 주요 분야 | 그린스마트시티학과 전공 프로젝트 |
| 디자인 톤 | 그린 + 화이트 |

### 지원 대상 기업·직무
- 【확인 필요: 지원하려는 기업/업종/직무】 — 강조할 프로젝트와 표현이 여기에 따라 달라짐

---

## 3. 인물 정보 (출처: `소개.txt`)

- 이름: 이동규
- 소속: 상명대학교 그린스마트시티학과 24학번
- 성장 배경: 부천 출생, 인천 5년, 수원 17년 거주
- 관심사: 축구, 야구

> **결정 (2026-09-22):** 웹 페이지에는 생년월일·거주 이력·취미를 그대로 두고,
> **기업 지원용 PDF에서는 제외**한다. PDF에는 이름·소속·학과·학번만 넣는다.

- 연락처: **PDF에 표기하지 않는다** (2026-09-22 결정).
  넣게 되면 `backend/data/profile.json` 에 `"contact": [{ "label": "이메일", "value": "주소" }]` 를 추가한다 (PDF 프로필 표에 자동으로 들어간다).
- 【확인 필요: 기술 스택·자격증·사용 가능한 툴 (예: GIS, CAD, SketchUp 등 실제 사용 경험)】
- 【확인 필요: 수상·대외활동·경력】

---

## 4. 프로젝트 목록

프로젝트 상세 내용은 **`projects.md`** 에 정리한다. 홈페이지·PDF에 프로젝트 내용을 쓸 때는 반드시 그 파일을 기준으로 한다.

- 뮤직 & 사운드 아카이브 (에코빌딩 프로젝트, 3인 팀) — `에코빌딩 9조 15주차 최종.pptx`
- 매립지 서식지 반환형 생태 복원과 IoT·스마트 수문 기술 접목 방안 (팀 프로젝트) — `IoT-스마트 수문 기술 접목 방안 최종.pdf`
- 수채공원 (디지털설계스튜디오Ⅱ, 4인 팀) — `디설스_수채공원 최종 llll.pptx`
- 손기정과 달리는 시간 (디자인설계스튜디오 스마트정원, 길동무팀 4인) — `길동무 발표자료.pptx`
- 【확인 필요: 추가로 넣을 프로젝트가 있는지】

---

## 5. 참고 기준 자료

### 용어집
- 전공 용어는 **【확인 필요: 기관명】의 용어집**을 기준으로 사용한다.
- 용어집에 없는 용어는 임의로 정의하지 않고 사용자에게 확인한다.
- 【확인 필요: 용어집 파일 또는 링크】

### 데이터 출처
- 프로젝트에 쓰인 데이터는 **토지정보【확인 필요: 정확한 데이터/시스템 이름】** 등이다.
- 모든 도표·지도·수치에는 **출처를 페이지 안에 표기**한다. 예: `출처: ○○기관, 데이터명(연도)`
- 【확인 필요: 사용한 데이터 목록과 각 제공 기관·기준 연도】

---

## 6. 페이지 구성 (PDF 기준)

PDF 한 장 = 하나의 섹션으로 관리한다. 페이지 번호 코드(`P01`, `P02`…)를 파일 주석과 대화에서 공통으로 사용한다.

| 코드 | 페이지 | 상태 |
|---|---|---|
| P01 | 표지 (이름·소속·수록 프로젝트 목록·출력일) | 웹 + PDF 반영 (자동 생성) |
| P02 | 프로필 (기본 정보 / 프로젝트 개요 표 / 사용 도구 / 키워드) | 웹 + PDF 반영 (자동 생성) |
| P03 | 프로젝트: 뮤직 & 사운드 아카이브 (에코빌딩) | 데이터(JSON) → 웹 + PDF 자동 반영 |
| P04 | 프로젝트: IoT·스마트 수문 생태 복원 제안서 | 데이터(JSON) → 웹 + PDF 자동 반영 |
| P05 | 프로젝트: 수채공원 | 데이터(JSON) → 웹 + PDF 자동 반영 |
| P06 | 프로젝트: 손기정과 달리는 시간 | 데이터(JSON) → 웹 + PDF 자동 반영 |

> PDF는 **표지 → 프로필 → 프로젝트 4건** 순서다. 프로젝트 1건은 새 장에서 시작하고,
> 내용이 길면 다음 장으로 이어진다 (현재 총 9장 분량).
> '나의 고향, 수원'과 '나의 여정'은 사용자 결정에 따라 **PDF에서 제외**한다 (웹에는 그대로 둔다).

### PDF 제작 시 규칙
- 용지 크기: **A4 세로** (`@page { size: A4 portrait; margin: 16mm 15mm; }`)
- PDF는 **웹 화면을 복제하지 않는다.** `frontend/js/pdf.js` 가 데이터에서 필요한 내용만 뽑아 문서용 DOM(`#pdf-doc`)을 새로 조립하고,
  인쇄할 때는 그 문서만 출력한다. 화면 요소(배경 애니메이션·네비게이션·모달·사진)는 인쇄에서 모두 빠진다.
- 문서 1장 = `.pdf-page` 하나. 소제목만 남고 내용이 다음 장으로 넘어가지 않도록 `break-after: avoid`를 쓴다.
- **인쇄 설정의 '배경 그래픽'을 켜지 않아도 읽히게** 만든다 → 채워진 배경색 대신 선(테두리)과 글자색으로 구분한다.
- 인쇄(PDF)에서는 애니메이션·호버 효과가 보이지 않으므로, 정보는 항상 정적인 상태로도 전달돼야 한다.
- 새 내용은 **`backend/data/projects.json` 에만 추가하면 웹 카드와 PDF에 모두 자동으로 들어간다** (같은 내용을 두 곳에 쓰지 않는다).

---

## 7. 현재 기술 현황

### 폴더 구조 (2026-09-22 프론트엔드 / 백엔드 분리)

```
frontend/        화면. 이 폴더만 GitHub Pages에 올라간다
  index.html     빈 틀 (내용 없음). 섹션 자리만 있고 JS가 채운다
  css/           tokens → base → layout → components → responsive → print 순서
  js/            config(설정) · api(통신) · dom(도구) · render/ · ui/ · pdf
  assets/        이미지 (영문 소문자 + 하이픈)
backend/         Node + Express API 서버
  data/*.json    ★ 내용의 원본 (profile · projects · hometown · journey)
  src/           routes → controllers → services → repositories
scripts/         build-site.js (배포용 dist/ 만들기)
.github/workflows/deploy-pages.yml   main push → Pages 자동 배포
```

### 어디를 고쳐야 하나

| 고치려는 것 | 고칠 파일 |
|---|---|
| 글·수치·프로젝트 내용 | `backend/data/*.json` (여기만) |
| 색·글꼴·간격 | `frontend/css/tokens.css` 의 `:root` |
| 화면 모양 | `frontend/css/components.css` 등 |
| 화면 그리는 방식 | `frontend/js/render/*.js` |
| PDF 문서 구성 | `frontend/js/pdf.js` + `frontend/css/print.css` |
| 서버 주소 | `frontend/js/config.js` 의 `apiBase` |
| 데이터를 어디서 읽을지 | `backend/src/repositories/index.js` |

### 실행

```
npm run setup    # 처음 한 번 (백엔드 라이브러리 설치)
npm start        # http://localhost:3000 — 화면 + API 함께
npm run build    # dist/ 만들기 (배포용)
```

### API

`GET /api/health` · `GET /api/portfolio` · `GET /api/projects` · `GET /api/projects/:id`
응답은 `{ "data": ... }`, 오류는 `{ "error": { "status", "message" } }` 모양.

### 데이터 흐름

화면 → `api.js` → ① 백엔드 API → 실패하면 ② `data/*.json` 직접 읽기
(GitHub Pages에는 서버가 없으므로 ②로 동작한다. 배포 시 `backend/data` 가 `dist/data` 로 복사된다.)

### 나중에 DB·외부 API 연결

`backend/src/repositories/` 안에서만 바꾼다. `db.repository.example.js` 를 복사해 채우고
`repositories/index.js` 의 `case 'db'` 주석을 푼 뒤 `DATA_DRIVER=db` 로 실행한다.
서비스·컨트롤러·화면 코드는 고치지 않는다.

### 기능

- 프로젝트 카드 → 누르면 상세 모달 (내용은 `render/detail.js` 가 만든 것을 함께 씀)
- 오른쪽 상단 **공유하기** → 링크 공유(주소 복사) / PDF 공유(구조화 문서 인쇄)
- Ctrl+P로 인쇄해도 같은 구조화 문서가 나온다

### 배포

- 저장소: https://github.com/donggu050731-sys/leeeddon (`main` 브랜치)
  - 예전 이름 `desktop-tutorial` 에서 바뀌었다. 옛 주소는 GitHub가 자동 연결해 주지만 새 이름을 쓴다.
- 주소: https://donggu050731-sys.github.io/leeeddon/
- `main` 에 push → GitHub Actions가 `frontend/` + `backend/data` 를 묶어 배포
- **한 번만 필요한 설정:** GitHub → Settings → Pages → Source 를 **GitHub Actions** 로 변경
- 저장소는 **공개** 상태 → 개인정보가 담긴 파일은 올리지 않는다 (1장 7번 원칙)

## 8. 변경 이력

- 그린+화이트 톤 UI 적용
- 수원 소개 배너 추가 (수원화성, 수원청개구리)
- GOAT 섹션(메시·호날두) 추가 후 삭제 — 기업 지원용에 맞지 않아 제외
- GitHub 업로드 및 GitHub Pages 배포
- CLAUDE.md 작성 (2026-09-15)
- projects.md 작성 — 원본 PPT·PDF 내용 요약 (2026-09-15)
- projects.md에 수채공원·손기정과 달리는 시간 추가 (2026-09-15)
- 웹페이지에 프로젝트 카드 섹션 추가 — 카드 클릭 시 상세 모달, 인쇄 시 자동 펼침 (2026-09-15)
- "내가 사랑하는 스포츠" 섹션 삭제 (2026-09-15)
- 오른쪽 상단에 '공유하기' 버튼 추가 — 링크 공유 / PDF 공유 (2026-09-22)
- PDF 추출 기능 추가 — 화면 복제가 아니라 카드 내용만 뽑아 A4 구조화 문서(표지·프로필·프로젝트 4장)로 생성 (2026-09-22)
- PDF 반영 범위 확정 — 생년월일·취미·수원·여정은 PDF에서 제외, 연락처 미표기 (2026-09-22)
- 프론트엔드 / 백엔드 분리 (2026-09-22)
  - 페이지 내용을 전부 `backend/data/*.json` 으로 옮김 (글자 그대로 옮겼고 내용 변경 없음)
  - 한 파일이던 `index.html` → `frontend/` (틀 · CSS 6개 · JS 모듈)로 나눔
  - Node + Express API 서버 추가 (`backend/`, 저장소 계층 분리 → DB 교체 대비)
  - 이미지 이름 정리: `suwon_hwaseong.jpg` → `frontend/assets/suwon-hwaseong.jpg` 등
  - GitHub Actions 배포 workflow 추가 (Pages Source 설정 변경 필요)
