{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "이동규 포트폴리오 — 프론트엔드(frontend) + 백엔드(backend)",
  "type": "module",
  "scripts": {
    "setup": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "build": "node scripts/build-site.js"
  },
  "engines": {
    "node": ">=20"
  }
}
